# ============================================================================
# Demoblaze Playwright Test Automation - Environment Configuration
# ============================================================================
# Companies House Assessment Reference: 437782
# 
# Only include settings here that you need to override.
# All defaults are defined in playwright.config.ts
# ============================================================================

# Target application URL (default: https://www.demoblaze.com/index.html)
# BASE_URL=https://www.demoblaze.com/index.html

# API base URL for API layer tests (default: https://api.demoblaze.com)
# API_BASE_URL=https://api.demoblaze.com

# Run tests headless (true) or with visible browser (false)
# (default: true)
# HEADLESS=true

# Number of parallel workers (default: 4 for local, 1 for CI)
# PARALLEL_WORKERS=4

# Retry attempts on failure (default: 0 for local, 2 for CI)
# RETRIES=0

# CI/CD environment flag - set to true in GitHub Actions
# (automatically adjusts workers and retries)
# CI=false

# ============================================================================
# Advanced overrides (rarely needed - defaults in playwright.config.ts):
# DEFAULT_TIMEOUT=60000
# EXPECT_TIMEOUT=10000
# ACTION_TIMEOUT=15000
# NAVIGATION_TIMEOUT=30000
# TRACE_MODE=retain-on-failure
# SCREENSHOT_MODE=only-on-failure
# VIDEO_MODE=retain-on-failure
# VIEWPORT_WIDTH=1920
# VIEWPORT_HEIGHT=1080
# REPORT_PATH=./playwright-report
# ============================================================================
